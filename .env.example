# Logger configuration (env → config → defaults)

# Logging mode: console | route | both | none
# LOG_MODE=console

# Remote logging endpoint (used in route/both modes)
# LOG_ROUTE_URL=http://localhost:3000/api/logs

# In-memory log cap (positive integer)
# LOG_MAX_LOGS=1000

# Stored sessions cap (positive integer)
# LOG_MAX_SESSIONS=100

# Comma-separated list of sensitive fields to redact
# LOG_SENSITIVE_FIELDS=password,token,secret,key,apiKey,auth,authorization

# Node environment: production disables console output automatically
# NODE_ENV=development
